<?xml version="1.0" encoding="utf-8"?>
<!--
  MosGuard (flavor `guard`) — permisos y componentes EXTRA que el YapeCaptor de producción NO trae.
  Solo se fusionan en el APK de MosGuard.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />

    <application>
        <!-- la cámara de resguardo: foto a pedido / cuadros en vivo. Foreground service tipo camera. -->
        <service
            android:name=".CamaraGuard"
            android:exported="false"
            android:foregroundServiceType="camera" />
    </application>
</manifest>
