node_modules/
package-lock.json

# clasp credentials — NUNCA commitear OAuth tokens
.clasprc.json
**/.clasprc.json

# Credenciales Supabase — NUNCA commitear
supabase/.pgpass
supabase/.temp/
inversionesmos-*.json

# Scripts de trabajo de migración (no son producto; usan .pgpass externo)
supabase/*.js
