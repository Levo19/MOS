{ "version": "2.43.219", "build": "20260615-SW-ROBUSTEZ-ROLLOUT.Fix#1 fetch handler:TODA rama de respondWith resuelve a Response valido(antes Promise.race con timeout que RECHAZABA podia dejar respondWith con promesa rechazada/no-Response->'Failed to convert value to Response').Ahora:timeout RESUELVE a null(no rechaza),fetch envuelto en .catch(()=>null),guards explicitos,Response 504 de ultimo recurso->NUNCA undefined,NUNCA rechazo sin manejar.No-GET(POST supabase rpc/get_flags)+cross-origin(firebase/gstatic)=passthrough nativo sin respondWith.Fix#2 rollout:importScripts FCM envuelto en try/catch(un blip de gstatic durante install ya NO aborta el SW->el dispositivo ya no queda pegado en version vieja por install fallido).skipWaiting+clients.claim+purga de caches viejas en activate YA estaban;network-first para html/app.js/api.js/version.json YA estaba(garantiza version fresca con red,fallback cache offline).node -c OK.NO toca logica de negocio ni flags(siguen en'0',INERTE)." }
